<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.adrian.gestionfct.controller.EstudianteController"
            prefHeight="600.0" prefWidth="900.0">

    <!-- TOP: Barra de búsqueda -->
    <top>
        <VBox spacing="10" style="-fx-background-color: #2196F3; -fx-padding: 15;">
            <Label text="Gestión de Estudiantes" 
                   style="-fx-text-fill: white; -fx-font-size: 20px; -fx-font-weight: bold;"/>
            
            <HBox spacing="10" alignment="CENTER_LEFT">
                <TextField fx:id="txtBuscar" promptText="Buscar por nombre o apellidos..." 
                          prefWidth="300" prefHeight="35"/>
                <Button fx:id="btnBuscar" text="🔍 Buscar" onAction="#handleBuscar" 
                       styleClass="btn-primary" prefHeight="35"/>
                <Button fx:id="btnLimpiar" text="Limpiar" onAction="#handleLimpiar" 
                       styleClass="btn-secondary" prefHeight="35"/>
            </HBox>
        </VBox>
    </top>

    <!-- CENTER: Tabla -->
    <center>
        <TableView fx:id="tableEstudiantes">
            <columns>
                <TableColumn fx:id="colId" text="ID" prefWidth="50"/>
                <TableColumn fx:id="colNombre" text="Nombre" prefWidth="150"/>
                <TableColumn fx:id="colApellidos" text="Apellidos" prefWidth="180"/>
                <TableColumn fx:id="colEmail" text="Email" prefWidth="200"/>
                <TableColumn fx:id="colCiclo" text="Ciclo" prefWidth="150"/>
                <TableColumn fx:id="colGrupo" text="Grupo" prefWidth="80"/>
            </columns>
            <placeholder>
                <Label text="No hay estudiantes registrados" 
                      style="-fx-font-size: 14px; -fx-text-fill: gray;"/>
            </placeholder>
        </TableView>
    </center>

    <!-- BOTTOM: Botones de acción -->
    <bottom>
        <HBox spacing="10" alignment="CENTER" style="-fx-padding: 15; -fx-background-color: #f5f5f5;">
            <Button fx:id="btnNuevo" text="➕ Nuevo" onAction="#handleNuevo" 
                   styleClass="btn-success" prefWidth="120" prefHeight="40"/>
            <Button fx:id="btnEditar" text="✏️ Editar" onAction="#handleEditar" 
                   styleClass="btn-primary" prefWidth="120" prefHeight="40"/>
            <Button fx:id="btnEliminar" text="🗑️ Eliminar" onAction="#handleEliminar" 
                   styleClass="btn-danger" prefWidth="120" prefHeight="40"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Button fx:id="btnVolver" text="← Volver" onAction="#handleVolver" 
                   styleClass="btn-secondary" prefWidth="120" prefHeight="40"/>
        </HBox>
    </bottom>

</BorderPane>